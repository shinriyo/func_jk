var Component = React.createClass(
getInitialSrate: funxtion()
{
// 表計算アプリのマス名に相当する
},
handleChange: function(event)
{
  var a = event.target.value * i; //
this.setState({b: a * 5});
},
render: function()
{ // aのマス目の値を監視すると同時に
  return <div>
}
)

